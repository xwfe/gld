# gld 当前任务与后续规划（0.8.0 之后）

更新日期：2026-09-25。**本文件是当前排队与认领入口**；审查与验收过程保留在
[生命周期审查](docs/reviews/2026-09-23-lifecycle-and-docs-audit.md)，用户用法见
[项目开发生命周期](docs/project-lifecycle.md)。下方“历史实施记录”只作追溯，不再当作当前待办。
沿用 D / L 原有归属，不新增平行的 Goal、Task 或执行状态系统。

## 当前基线与判断

本轮起点 `main@eefdc1b40015`、工作树干净；运行服务 0.8.0、构建 `09b1ae57d73a`，
连接工具表 28 项、指纹 `9ed41391e27553c6`。这两个提交之间只改了任务计划和审查记录，
不是服务漏部署了运行代码。当前连接已暴露此前缺失的工具及参数，并实际调通 `check_command`。
复核前没有活动 Task，Planning 为 direct、没有聚焦 Goal / Plan；说明当前靠 Markdown 排队，
不是“没有计划”，也不要求为此再建一套机器状态。

**从集中修缺陷转入日用稳定与场景验收，不开始另一轮大重构。** 任务验收、基线恢复、
失败传播、grant、运行记录、发布来源证明都已有实现。下一步补证据和使用断点，
不把“可以执行任意项目脚本”包装成无人值守的完整开发平台。

## 已交付，不重新立项

| 范围 | 当前结论 | 保留的边界 |
| --- | --- | --- |
| U0–U5、G1–G3、RFC-0004/0005/0006 | 工具可靠性、单服务多项目、Skills、Notebook、MCP 转发已落地 | 具体客户端/远端链路仍按实际证据判断，不重复造功能 |
| D01–D03、D11 | 正式验收、基线复核接纳、命令终态传播、任务存储并发与损坏处理已修 | 退出 0 不证明测试充分；暂停不等于停止已有命令；不是强断电持久性保证 |
| D04–D06、D14 | 工具表核对、结果保真、文档门禁、Git 文件清单已修 | 当前连接的旧 schema 缺失已消除；其他客户端不能直接外推 |
| D07–D08 | 最小项目 grant 和真实安全标注已交付 | grant 默认只读；`--write` 只给可信主体，不是 OS 隔离；grant 无本机 MCP/远端写能力 |
| D09 | 结局与有界输出落盘，重启后可按已知 id 读取 | 不恢复进程、不重放；跨重启旧结果不自动成为有效验收证据 |
| D12–D13 | 0.8.0 发布流程、校验和、来源证明已有记录；既有协议回退已核对 | 下载包验收必须匹配版本；协议回退不是全面支持新协议 |

`gld tool call` 的 CLI 退出码仍表示工具调用是否成功，这是已确认的契约，**不要再次当缺陷改掉**；
命令结果继续看 `command_ok` / `exit_code`。

## 当前待办队列

| 顺序 / 归属 | 状态 | 下一步与完成标准 |
| --- | --- | --- |
| 先补验收：D12 / 0.8.0 glibc 下载包 | 已产出，待当前版本实跑 | 0.7.0 的 Debian 证据不能替代 0.8.0。对下载的 0.8.0 包核验来源后，在匹配的 Linux 环境跑版本、IPC、命令/输出、正常停止与运行记录读取。上次因镜像下载失败未验；本轮没重试该网络依赖，不假定现在仍故障 |
| 先补验收：D07 / ChatGPT 只读 grant | 已实现，待客户端验收 | 需要用户在独立测试连接上用 grant 口令授权；确认只见授权项目、拒绝写操作和范围外访问、撤销后不能续用，同名重建不恢复旧授权。不替换现有主凭据连接，不输出令牌 |
| 平行补门禁：D12 / Windows 最小运行测试 | 尚缺运行证据，需补可运行测试 | 利用已有 Windows CI runner，先验下载包/构建包起停、命名管道、文件锁、命令终态与运行记录。若只跑构建包，明确不能算下载包验收；适配 Unix-only fixture 后按小组增加，不一次打开全套，也不以“没有本机 Windows”无限延期 |
| 下一项开发：D09 / 运行记录发现 | 待开发，建议优先 | 在不知道 session_id 时也能发现本项目、本主体的运行记录；有界分页与状态/时间筛选，选择记录后复用现有输出读取。不得再造 Job 调度、自动重放或凭 PID 接管 |
| 下一阶段：D10 / 真实项目开发验收 | 待开发，按下节分步推进 | 先用一个 Rust CLI 与一个 TypeScript Web 场景串起读改测审，再补项目任务发现、运行准备、浏览器证据；遇到断点才增加通用能力 |

前两项是**补证据，不是重写功能**；等待用户授权或测试环境时可以平行推进 D09 和 Windows
测试适配，不能把环境阻塞当成扩大产品范围的理由。所有部署、grant 创建/撤销、CI 推送和
发布仍须取得对应授权；本轮只更新规划，不执行这些动作。

## D09 下一项开发的最小范围

复用 `crates/core/src/tools/runs.rs` 的记录、主体检查和配额规则，给 MCP 增加最小只读发现
入口，CLI 只做同一用例的薄封装；具体命名在实现前核对现有命令，不在本文冒充已可用。
列表只给脱敏摘要、id、状态、时间、输出完整性和来源，不回日志全文，不跨主体/跨项目枚举。
分页有稳定顺序与明确的过期/变化反馈；先授权过滤再计数和分页，不能从总数泄露别人的记录。

验收重点：同主体重连/重启后的发现、跨主体猜 id、只读 grant 的范围、记录过期/淘汰/损坏、
`unknown` 不误报成功也不发信号杀旧 PID。未知结果、掉盘失败、日志截断分别说明；**历史记录
可发现不等于旧测试重新有效**，继续走 D01 的验收规则。

不同 OAuth 注册、服务主凭据、grant、CLI 并非天然同一个主体。CLI 不能为了便利默认越过
主体边界；需要操作员管理视图时另行设计，不混入普通只读查询。

## D10 按真实场景推进，而非堆工具

**先用现有能力建立可回放的流程。** Rust 场景覆盖需求/计划、修改、失败测试、修复、复测、
diff、文档与 Task 验收；Web 场景再覆盖 dev server、业务就绪、关键页面操作、错误与截图、
修复后复测。独立 fixture/测试账号，不把这一步等同于修改现有业务项目或生产部署。

**再补任务发现和运行准备。** 第一版只读识别 Cargo 与 package.json 中已声明的入口，
返回命令来源、工作目录、所需工具/环境和歧义；发现不执行，不自动安装依赖或信任仓库配置。
复用现有 exec / run / output，加必要的就绪检测与服务归属，不引入第二个执行器。
gld 自己的 MCP 服务就绪，和被开发项目的业务服务就绪，是两个不同对象。

**最后补浏览器与制品证据适配。** 复用获准的浏览器 MCP/项目 E2E 脚本；绑定正确执行节点、
测试 origin、被测代码版本、命令与截图/console/network/trace/report。先把报告保存为受限
项目文件并关联任务，不先造通用上传下载平台。修代码后旧证据失效；截图不能替代错误检查。
浏览器与本机 MCP 不继承项目沙箱，不能默认接管个人登录态或公开 dev 端口。

通过 ccnm 的场景另验真实 SSH、会话寿命和节点归属；本机管道测试不替代它。
RFC-0005 尚留有远端用户级 Skill 附件的未验记录，接入此场景时先核对是否已有后续证据，
没有再补，不重做 Skills。租约可见性/续租、嵌套参数能力协商仍按真实远端需求评估，
不能把远端后台命令升级为永久 Job。

## 条件触发，不排进默认近期开发

| 项 | 触发条件 / 处理原则 |
| --- | --- |
| 代码签名、公证/平台信任体验 | 面向普通用户分发时取得相应账号与证书后推进；构建来源证明已完成，不能再与代码签名合并成未做 |
| D13 新一代协议实现、动态工具变更通知 | 有明确目标客户端需要且已有兼容用例时；当前回退测试保留，不因版本日期较新就迁移 |
| grant 有效期、范围原地调整、服务端审批 | 真实共享/撤销场景需要时单独设计；只读 grant 已交付，不能把这些增强写成“grant 尚未实现” |
| Harness 锁等待上限、断电耐久性、记录清理/配额可配置 | 出现长时阻塞、恢复或磁盘需求时补专项；当前缺口见审查 §7，不能宣称已有严格上限/强耐久保证 |
| 语义导航、managed worktree、CI/PR 写操作、Agent 委派 | 有真实用例和授权后分别接入；不重做 toexec 基础实现，不把模型主循环塞进 gld |

真实 PTY、另起单项目 MCP 主服务、第二套 Goal/Plan/Task、完整 IDE/部署平台不纳入。
没有必要为了补齐旧路线 L0–L6 的所有格子而无限扩展产品。

## 旧路线如何归并

L0 的原工具修复收口；L1 的最小 grant / Runtime 所有权已落地但不代表多租户强隔离；L2 已有
验收与运行记录，剩余归 D09 查询与 D10 任务发现/运行准备；L3 归 D10 浏览器流程；L4 的
Skills/Notebook/MCP 已做，语义导航按需；L5 的 gld 自身发布流程已有证据，不等于能代管
任意项目的发布，协作增强按需；L6 仍是经 ccnm 的可选委派，不是基础版本门禁。

每次接手先读本文件当前队列、目标 D 项的最新实施段、当前源码与 diff；认领一个可验收的
小项，不按旧缺陷标题批量开工。当前状态只改本文件；审查追加事实和证据，不再维护另一张
独立待办表。版本号由实际交付和兼容性变化决定，不先许诺一次做完“0.9 全生命周期”。

## 本轮复核证据（2026-09-25）

隔离 `GLD_HOME=target/replan-20260925-test-home`，当前源码执行
`cargo test --workspace --all-targets --locked -- --quiet --nocapture`，退出 0，框架汇总
**853 passed / 0 failed / 0 ignored**。其中
`share_reports_the_missing_binary_instead_of_a_silent_no_url` 因本机已安装 cloudflared 提前
返回，**缺程序的分支本机未覆盖**；ccnm 本地真实管道组 3 项运行 8.29 秒，没有缺二进制的
跳过提示，不冒充真实 SSH。以后补门禁时应让环境依赖用例可隔离复现，不能拿 0 ignored
掩盖提前返回。运行服务的构建与工具表、原生预检已实查；本轮不重验远端 CI 或下载包。

## 历史实施记录（2026-09-23 至 2026-09-25）

以下保留原有勾选用于追溯；其中“未做/不纳入”按当时阶段理解，排队以本文上半部分为准。
详细结果在[审查 §7](docs/reviews/2026-09-23-lifecycle-and-docs-audit.md#7-处理进展)。
上一轮“固定隧道启动检查”的勾选在 git 历史里（`5cc3389` 之前）。

## 第二轮（已提交 69321e6 / 2e9fa7e / 103528b）

- [x] D03 命令失败状态传播；D01 任务正式验收；D02 基线恢复；D04 客户端工具表核对。

## 第三轮

- [x] `gld tool call` 命令失败时仍退出 0：按决定不改，写进帮助并用测试钉住。
- [x] D14 `git_diff` / `git_show` 文件清单：直接问 git（`--name-status` / `--numstat -z`）。
- [x] D06 文档生成门禁：失败即停、临时文件后替换、隔离 HOME、查空表；全部文档链接检查。
- [x] D05（gld 侧）：输出过期不再叫人直接重跑。
- [x] D05（toexec 侧）：`shape` 只在确定是副本时省掉结构化结果；toexec 本地提交 `bfa809d`。
- [x] D05 收尾：经批准推送 `toexec-mcp-v0.2.1` tag，gld 升级依赖，提交 relay 契约测试与文档。

## 第四轮

- [x] D11 任务存储：工作区级文件锁、临时文件带进程号并 sync；坏任务文件报 `STORE_CORRUPT` 并停写；
  日志坏行跳过并报行号、半行先隔开；索引坏了按任务文件重算。旧代码上先复现再修。

- [x] D13 核对：新版（2026-07-28）客户端先探测 `server/discover`，gld 回 200 + `-32601`，客户端退回
  `initialize`。官方 TS SDK 2.0 实连通过；不改服务端，用测试钉住回法。

## 真机

- [x] 本机升级到 0.7.0（协议 4），Client ID、口令、凭据、ChatGPT 注册的客户端逐项一致；文档写清升级、
  同版本号不提醒重启、ChatGPT 要到 chatgpt.com/plugins 点 Refresh。
- [x] ChatGPT 在 `~/xdw/gld-realtest` 上走完验收、失败拒收、外部修改后先看再接纳、`git_diff` 文件清单。
- [x] 修掉验收中发现的：被拒调用的操作记录没挂任务。

## 收尾

- [x] 推送全部提交；清理真机项目、审查临时证据、旧打包产物和中间备份；改正 `gld rm` 的说法。

## 发布

- [x] D12：发版前核对版本、手写发布说明、musl 必须编过；本机打包与回滚演练；Release 空跑全绿。
- [x] 经批准推送 `v0.7.0`，Release 出来后下载全部包核对校验和；macOS 两个、musl 实跑 `--version`。

## D08 安全语义（2026-09-24）

- [x] `request_permissions` 任何模式都不发授权（dangerous 下以前回 granted，确认门其实一个没放）。
- [x] 退役 `compat-readonly-all`：设置时报错，老配置读成 advanced、标注照实。
- [x] 四个工具的 `confirm` 参数写明服务端核实不了用户批准。

## D07 项目级授权（2026-09-24，RFC-0007）

- [x] 鉴权层：grant 口令授权、令牌带 grant id、验令牌 / 换码 / 刷新都查 grant 还在；bearer 认 grant 令牌。
- [x] hub：范围外项目看不见；只读 grant 收窄到 read-only 工具集；不给本机 MCP 转发；远端只读。
- [x] `gld grant add / ls / rm`，rm 停掉它起的命令；协议号 5；端到端测试走真的 OAuth 与 bearer。
- [x] 独立审查 9 条：默认只读 + `--write`、回滚后 grant 令牌 401、Git 收窄到项目子目录、confine-reads 项目不给 grant、
  grant 不开远端写会话、撤销后 35 秒再清一次、有 grant 时拒绝改 noauth 等。
- [x] 推送 10 个提交（CI 全绿）；本机服务升级到 `e754c51`，凭据与注册客户端逐项指纹一致；机器重启后 `gld daemon start` 恢复。
- [x] 文档：connect-clients.md 四档表（什么都不用做 / 点 Refresh / 重新授权 / 删了重建）为唯一权威；install.md 升级
  多一步比工具表指纹；development.md 要求发布说明写明连接器要不要动；排障、README、daemon 同步。
- [x] ChatGPT 点 Refresh 后核对新工具表（2026-09-25，升到 0.8.0 后：回包 36156 字节与服务当前表一致）。
- [ ] grant 口令在 ChatGPT 授权页上实测（要用户在网页上操作）。
- [x] 开机自启：四步命令写进 daemon.md（临时 launchd 任务实测后撤掉），本机由用户自己配。
- [x] 重启后 cargo 找不到：补全局可执行路径；修 `gld tool call` 上下文缓存不随全局设置失效（`432dfd7`）。

## D12 尾巴（2026-09-24）

- [x] v0.7.0 glibc 包在 Debian 12 amd64 容器实跑；Linux 上 IPC 正常。
- [x] 查出并修掉后台命令在守护进程退出 / 直连命令行退出后成孤儿（`4ffd96f`），CI Ubuntu、macOS 全绿。
- [x] 发布流水线加构建来源证明（`4216d21`）。
- [x] 来源证明第一次真正签出来：经批准用 gh 手动空跑 Release（run 35962328940，`bc8e13a`），5 个包都验过，改字节、换签名流水线都被拒。
- [ ] 代码签名（付费证书）、Windows 包实跑（没有 Windows 机器）。

## D09 运行记录（2026-09-24）

- [x] 旧代码上先复现：守护进程 stop/start、`kill -9`、直连下一条命令行，`read_output` 都报 `SESSION_NOT_FOUND`。
- [x] `GLD_HOME/runs/`：结局与分段日志落盘、按主体读、配额 64 条 / 7 天；gld 退出记 `interrupted`，没来得及记的判 `unknown`。
- [x] 查出并修掉：守护进程退出时经服务起的命令被记成 `killed`。
- [x] 文档：concepts、lifecycle、排障、daemon、security、architecture、README。
- [x] 独立审查 9 条逐条处理：从记录读的写入次数按起跑计数现算（重启后为 null）、Windows import、全项目清理与测试隔离、
  补 KILL、kill 与自然退出的竞态、退出提示只数真停掉的等。
- [x] 推送 4 个提交，CI run 35961573998 全绿（Ubuntu、macOS 跑了新端到端测试，Windows 编过）。
- [x] 经批准升级本机服务到 `bc8e13a`（指纹逐项一致，ChatGPT 不用 Refresh）；ChatGPT 实测三段：正常重启后
  `interrupted`、重启前的证据被拒、重跑后 `completed`、`kill -9` 后 `unknown` 且孤儿不被误杀。测试项目已清理。

## 发布 0.8.0（2026-09-25）

- [x] 版本号、发布说明、daemon / install 里的版本；全量测试、本机打包实跑（`09b1ae5`）。
- [x] 经批准推送、空跑 Release 全绿后推 `v0.8.0`；下载 5 个包校验和、来源证明全过，macOS 两个与 musl 实跑。
- [x] 本机服务换成 0.8.0 发布包，工具表、`gld ls`、凭据与注册客户端指纹一致。
- [ ] glibc 包实跑（Docker Hub 连不上，等网络好了再拉 amd64 镜像）。

上述阶段结束后的剩余项已归并到本文的当前队列与条件触发表。
来源证明、0.7.0 glibc 包实跑已有证据，不再列作未做；0.8.0 glibc 包仍需本版本验收。
